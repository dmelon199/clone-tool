// const grabCaseDetailsAndClone = () => {
// 	// grab case details
// 	//let childCase = new Object();
	
//   const childCaseNumber = this.template.querySelector('slot[name="primaryField"][records-highlights2_highlights2]');
// 	// childCase.creator = $('#CreatedBy_ileinner > a').text();
// 	// childCase.status = $('#cas7_ileinner').text();
// 	// childCase.statusReason = $('#00N0L000006gk5T_ileinner').text();


	// store case details in sessionStorage so they can be used later
	// sessionStorage.setItem('childCaseNumber',childCase.number);
	// sessionStorage.setItem('childCaseCreator',childCase.creator);
	// sessionStorage.setItem('childCaseStatus',childCase.status);
	// sessionStorage.setItem('childCaseStatusReason',childCase.statusReason);

// 	console.log(childCaseNumber)
// }


// grabCaseDetailsAndClone();

const grabCaseDetailsAndClone = () => {

	const tempDOM = document.createElement('div');
	tempDOM.innerHTML = document.body.innerHTML;
}

grabCaseDetailsAndClone();